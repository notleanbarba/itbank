{
  "scripts": {
    "sass": "sass -w src/app/public/sass/:src/app/public/sass/",
    "live-server": "live-server --port=3010 --mount=/:src/app/",
    "start": "npm run sass & npm run live-server",
    "start-windows": "START /B npm run sass & npm run live-server"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.77.8"
  }
}
